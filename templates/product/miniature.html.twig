<div class="product-miniature link-finder">
    <div class="product-miniature_content">
        <img class="product-miniature_img" src="/uploads/{{ product.imgMiniature }}" alt="{{product.name}}">
        <a href="{{ path('product', { 'slug' : product.slug }) }}"></a>
        <p class="product-miniature_title">{{product.name}}</p>
        <span class="product-miniature_price">{{(product.price / 100)|number_format('2', ',', '.')}} €</span>
    </div>
</div>