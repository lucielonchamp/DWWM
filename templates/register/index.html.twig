{% extends 'base.html.twig' %}
{% block title %}S'inscrire - {{ contact_company }}
{% endblock %}
{% block content %}

<div class="container">
	<div class="formulaire col-md-6">
		<div id="register-form">
			<p class="h2">Inscription</p>

			{% if notification %}
				<div class="alert alert-info">{{ notification }}</div>

			{% endif %}

			{{ form(form) }}
			<div class="buttons">
				<p><a class="btn btn-second" href="{{ path('app_login') }}">Me connecter</a></p>
			</div>
		</div>
	</div>
</div>


{% endblock %}
